apiVersion: v1
kind: Service
metadata:
  name: {{ include "mnm.fullname" . }}
  labels:
    {{- include "mnm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "mnm.selectorLabels" . | nindent 4 }}
